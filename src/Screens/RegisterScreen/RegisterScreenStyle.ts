import { StyleSheet } from "react-native";
import * as s from "../LoginScreen/LoginScreenStyle";

export const styles = StyleSheet.create({
	...s.styles,
	errorText: {
		color: "red",
		fontSize: 12,
	},
});
